#include "Car.h"
#include "Route.h"
#include "PayRoutes.h"
#pragma once
using namespace std;

int main()
{

	Route RR;
	//RR.pop(0);
	//Car CC(2, 4);
	//RR.push_back(CC, 0);

		
	PayRoutes PP(5, 1);
	return 0;
}